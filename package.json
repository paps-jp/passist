{
  "name": "passist",
  "version": "0.1.0",
  "private": true,
  "description": "PAssist — share and remotely control a single window via a browser; no account required.",
  "scripts": {
    "postinstall": "npm --prefix server install && npm --prefix host install",
    "start": "npm --prefix host start",
    "server": "npm --prefix server start",
    "rebuild": "npm --prefix host run rebuild",
    "test": "node host/trust.test.js",
    "test:signaling": "node server/test-signaling.js"
  }
}
